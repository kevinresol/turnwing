normal-hello = Hello, { $name }!
normal-empty = Hello, World!
normal-bool =
    { $value -> 
        [0] No
       *[1] Yes
    }

getter-hello = Hello, { $name }!
getter-empty = Hello, World!
getter-bool =
    { $value -> 
        [0] No
       *[1] Yes
    }

const-hello = Hello, { $name }!
const-empty = Hello, World!
const-bool =
    { $value -> 
        [0] No
       *[1] Yes
    }
